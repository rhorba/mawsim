{
  "name": "mawsim",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r --filter '!@mawsim/db' exec vitest run --coverage",
    "test:e2e": "pnpm --filter web exec playwright test",
    "test:e2e:ui": "pnpm --filter web exec playwright test --ui",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "db:migrate": "pnpm --filter @mawsim/db migrate",
    "db:seed": "pnpm --filter @mawsim/db seed",
    "db:studio": "pnpm --filter @mawsim/db studio",
    "worker": "pnpm --filter @mawsim/db worker"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@vitest/coverage-v8": "^2.1.8",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["@biomejs/biome", "argon2", "esbuild", "sharp"]
  }
}
